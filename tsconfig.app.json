{
    "compilerOptions": {
        "paths": { "@src/*": ["${configDir}/src/*"] },
        "types": ["node"]
    },
    "exclude": [
        ".vitest/",
        "coverage/",
        "dist/",
        "node_modules/",
        "**/*.test.ts"
    ],
    "extends": "./tsconfig.base.json",
    "include": ["src/**/*.ts"]
}
